---
description: linka componente in components/index.html
---

Workflow per creare un link a un componente da `components/index.html`.

## Prerequisiti
- Il file {nome-componente}.html deve esistere in `components/`

## Passaggi

**Aggiungi link** in `components/index.html` (in ordine alfabetico nella lista)

